
{
  "entities": {
    "Product": {
      "title": "Product",
      "type": "object",
      "description": "Fixed-price cleaning supplies and equipment.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "price": { "type": "number" },
        "regularPrice": { "type": "number" },
        "categoryId": { "type": "string" },
        "imageUrl": { "type": "string" },
        "stockQuantity": { "type": "number" },
        "brand": { "type": "string" },
        "status": { "type": "string", "enum": ["Active", "Inactive"] },
        "variants": { "type": "array", "items": { "type": "object" } },
        "features": { "type": "array", "items": { "type": "string" } },
        "specifications": { "type": "array", "items": { "type": "object" } }
      },
      "required": ["id", "name", "price", "categoryId"]
    },
    "HeroBanner": {
      "title": "HeroBanner",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "imageUrl": { "type": "string" },
        "title": { "type": "string" },
        "subtitle": { "type": "string" },
        "buttonText": { "type": "string" },
        "buttonLink": { "type": "string" },
        "isActive": { "type": "boolean" },
        "order": { "type": "number" }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{uid}": { "schema": "#/entities/User", "description": "Unified user accounts." },
    "/products/{productId}": { "schema": "#/entities/Product", "description": "Inventory items." },
    "/product_categories/{id}": { "schema": "object", "description": "Product groupings." },
    "/brands/{id}": { "schema": "object", "description": "Product brands." },
    "/variant_types/{id}": { "schema": "object", "description": "Reusable variant types." },
    "/reusable_features/{id}": { "schema": "object", "description": "Standard features." },
    "/reusable_specs/{id}": { "schema": "object", "description": "Technical specification keys." },
    "/services/{serviceId}": { "schema": "#/entities/Service", "description": "Main services." },
    "/sub_services/{id}": { "schema": "object", "description": "Service add-ons." },
    "/hero_banners/{id}": { "schema": "#/entities/HeroBanner", "description": "Home page hero slides." },
    "/site_settings/{id}": { "schema": "object", "description": "Global config & UI." },
    "/bookings/{id}": { "schema": "object", "description": "Service appointments." },
    "/orders/{id}": { "schema": "object", "description": "Product sales." },
    "/payment_methods/{id}": { "schema": "object", "description": "Gateway configs." },
    "/coupons/{id}": { "schema": "object", "description": "Discount codes." },
    "/employee_profiles/{id}": { "schema": "object", "description": "Staff data." },
    "/staff_availability/{id}": { "schema": "object", "description": "Staff live status." }
  }
}
