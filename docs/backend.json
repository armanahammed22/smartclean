
{
  "entities": {
    "LandingPage": {
      "title": "LandingPage",
      "description": "Configuration for a dynamic product or service landing page.",
      "type": "object",
      "properties": {
        "slug": { "type": "string" },
        "type": { "type": "string", "enum": ["product", "service"] },
        "title": { "type": "string" },
        "active": { "type": "boolean" },
        "bannerImage": { "type": "string" },
        "productIds": { "type": "array" },
        "packages": { "type": "array" },
        "addOns": { "type": "array" }
      }
    },
    "OrderLandingProduct": {
      "title": "OrderLandingProduct",
      "description": "Order record from a product landing page.",
      "type": "object",
      "properties": {
        "pageId": { "type": "string" },
        "customerName": { "type": "string" },
        "totalPrice": { "type": "number" },
        "status": { "type": "string" }
      }
    },
    "OrderLandingService": {
      "title": "OrderLandingService",
      "description": "Order record from a service landing page.",
      "type": "object",
      "properties": {
        "pageId": { "type": "string" },
        "customerName": { "type": "string" },
        "totalPrice": { "type": "number" },
        "status": { "type": "string" }
      }
    },
    "ProductQNA": {
      "title": "ProductQNA",
      "description": "Customer questions and answers for products.",
      "type": "object",
      "properties": {
        "productId": { "type": "string" },
        "question": { "type": "string" },
        "answer": { "type": "string" },
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "status": { "type": "string", "enum": ["Pending", "Approved"] },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/landing_pages/{id}": { "schema": "LandingPage", "description": "Dynamic landing pages." },
    "/orders_products/{id}": { "schema": "OrderLandingProduct", "description": "Product sales orders." },
    "/orders_services/{id}": { "schema": "OrderLandingService", "description": "Service booking orders." },
    "/product_qna/{id}": { "schema": "ProductQNA", "description": "Product Q&A entries." }
  }
}
